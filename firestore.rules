rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Relaxed rules for development preview to ensure data persists across all devices
    // regardless of Firebase Auth configuration or email bypass states.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
